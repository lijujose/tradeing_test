name: try github scripts

on:
  push:
    branches:
      - master

jobs:
  try-github-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@0.2.0
        with:
          github-token: ${{github.tokenzx}}
          script: |
            console.log(context)
            console.log("*******", github.token, "************")
            console.log(Object.keys(github.pullRequests))
            const owner = context.payload.sender.login;
            const repo = context.payload.repository.name;
            const status = await github.pulls.get.endpoint({
              owner,
              repo,
            });
            console.log(status, owner);
